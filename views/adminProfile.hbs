{{> headers}}

{{!-- <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"> --}}

<section id="content" class="container my-5">

    <div class="row">
        <!-- User Details Section -->
        <div class="col-md-5">
            <div class="card p-4 shadow-sm">
                <div class="text-center mb-3">
                    <img src="/{{adminprofile.profilePicture}}" alt="Admin Profile" class="img-fluid rounded-circle" style="width: 150px; height: 150px;">
                </div>
                <h4 class="text-center">{{adminprofile.name}}</h4>
                <p><strong>Email:</strong> {{adminprofile.email}}</p>
                <p><strong>Phone:</strong> {{adminprofile.phone}}</p>
                <p><strong>Role:</strong> {{adminprofile.role}}</p>
                <p><strong>Status:</strong> {{adminprofile.status}}</p>
            </div>
        </div>

        <!-- Update Profile Form Section -->
        <div class="col-md-5">
            <div class="card p-4 shadow-sm">
                <h4 class="mb-4">Update Profile</h4>
                <form action="/admin/updateprofile/{{adminprofile._id}}" method="POST" enctype="multipart/form-data">
                    <div class="mb-3">
                        <label class="form-label">Name</label>
                        <input type="text" name="name" class="form-control" value="{{adminprofile.name}}" required>
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Email</label>
                        <input type="email" name="email" class="form-control" value="{{adminprofile.email}}" required>
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Phone</label>
                        <input type="text" name="phone" class="form-control" value="{{adminprofile.phone}}" required>
                    </div>

                   <div class="mb-3">
                       <label class="form-label">Status</label>
                      <select name="status" class="form-control">
                      <option value="Active" {{#if (eq adminprofile.status "Active")}} selected {{/if}}>Active</option>
                        <option value="Inactive" {{#if (eq adminprofile.status "Inactive")}} selected {{/if}}>Inactive</option>
                     </select>
                    </div> 

                     <div class="mb-3">
                        <label class="form-label">ProfilePicture</label>
                        <input type="file" name="newprofilepic" class="form-control" >
                    </div>
                    

                    <div class="mb-3">
                        <label class="form-label">New Password</label>
                        <input type="password" name="newPassword" class="form-control" placeholder="Enter new password">
                    </div>

                    <button type="submit" class="btn btn-primary">Update Profile</button>

                    
                </form>
 
            </div>
        </div>
    </div>

</section>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
