{{>headers}}
{{!-- 
<style>
  table {
    width: 100%;
    padding: 20px;
    border-collapse: collapse;
    margin: 20px 0;
    font-size: 16px;
    font-family: Arial, sans-serif;
    text-align: left;
    box-shadow: 0 2px 8px rgba(0,0,0,0.1);
  }

  thead {
    background-color: #4CAF50;
    color: white;
  }

  th, td {
    padding: 12px 15px;
    border: 1px solid #ddd;
  }

  tbody tr:nth-child(even) {
    background-color: #f9f9f9;
  }

  tbody tr:hover {
    background-color: #f1f1f1;
    cursor: pointer;
  }

  img {
    border-radius: 8px;
    object-fit: cover;
  }

  caption {
    font-size: 20px;
    margin-bottom: 10px;
    font-weight: bold;
  }
  #btn-div{
margin-left: 200px;

  }
  #btn{
    background-color: #4CAF50;
      color: white;
      border: none;
      padding: 10px 20px;
      border-radius: 8px;
      font-size: 16px;
      cursor: pointer;
      display: flex;
      align-items: center;
      gap: 8px;
      transition: background 0.3s ease;
  }
</style>
  --}}
<div>
  <div id="btn-div">
 <a href="/admin/addproducts"> <button  id="btn"
    <i class="fa fa-user-plus"></i> AddProduct
  </button></a>
</div>
  <table id="example21">
    <thead>
      <tr>
        <th>#</th>
        <th>Image</th>
        <th>Product Name</th>
        <th>Description</th>
        <th>Regular Price</th>
        <th>Sale Price</th>
        <th>Category</th>
        <th>Vendor</th>
        <th>Created At</th>
        <th>Stock Status</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      {{#each products}}
      <tr>
        <td>{{inc @index}}</td>
        <td><img src="/{{this.images.[0]}}" style="height: 80px; width:80px;" alt=""></td>
        <td>{{this.title}}</td>
        <td>{{this.description}}</td>
        <td>{{this.regularprice}}</td>
        <td>{{this.saleprice}}</td>
        <td>{{this.category}}</td>
        <td>{{this.vendor}}</td>
        <td>{{this.createdAt}}</td>
        <td>{{this.stockstatus}}</td>
        <td>
            <div>
                <a href="/admin/viewproduct/{{this._id}}">
                    <i class="fa-solid fa-eye"></i>

                </a>
                <a href="/admin/getupdateproduct/{{this._id}}">
                    <i class="fa-solid fa-pen-to-square"></i>
                </a>
                <form action="/admin/deleteproduct/{{this._id}}" method="POST">
                <button type="submit">
                    <i class="fa-solid fa-trash"></i>
                    </button>
               
                </form>

            </div>
        </td>
      </tr>
      {{/each}}
    </tbody>
  </table>
</div>

{{>footer}}
